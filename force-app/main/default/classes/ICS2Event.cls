public with sharing class ICS2Event {
    public ICS2Event() {

    }
    @InvocableMethod(label='Converts iCalendar to Events' description='Accepts ICS file as a string, extracts VEVENT structures and saves them as Salesforce Events')
    public static String[] convertICS(String[] input) {
        String result1 = '';
        
    }
}
